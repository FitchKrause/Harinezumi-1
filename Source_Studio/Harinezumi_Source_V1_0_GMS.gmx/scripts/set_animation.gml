/// set_animation(index, sprite, speed, end, loop, loop count, next, next frame)
 // Create and set a new animation.
 
    if(animation_index = argument[0])
    {
       animation_sprite_id  = argument[1];
       animation_speed      = argument[2];
       animation_end        = argument[3];
       animation_loop       = argument[4];
       animation_loop_timer = argument[5];
       animation_next       = argument[6];
       animation_next_frame = argument[7];       
    }

