/// tail_apply_animation(sprite, speed, x, y, angle, direction);
 // Script used to apply the tail parts for Miles.

    if(argument0 != noone)
    {
       miles_tail_sprite     = argument0; 
       miles_tail_speed      = argument1; 
       miles_tail_x          = argument2;
       miles_tail_y          = argument3; 
       miles_tail_angle      = argument4; 
       miles_tail_direction  = argument5;  
    }
    else
    {
       miles_tail_sprite     = noone;
    }
