/// align_roll_animation();
 // Aligns the roll sprite. You might need to tweak this depending on your sprite.
  
    draw_x -= ((3-(1*(angle==90)))*dsin(angle));
    draw_y -= ((2+(1*angle!=0))*dcos(angle));
